/**
 * Public type surface (`node-telegram-bot-api/types`).
 *
 * v2 ships the hand-authored excerpt in `./v2`; the full generated schema
 * (`./schemas`, v1-shaped) is retained on disk for the generator to evolve into
 * the v2 shapes (discriminated Update + Json<T> aliases + BotApi map).
 */
export * from "./v2.js";
